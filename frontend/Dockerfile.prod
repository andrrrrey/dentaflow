# ============================================================
# DentaFlow Frontend — Production multi-stage build
# Результат: собранная статика в nginx-контейнере
# ============================================================

FROM node:20-alpine AS build
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
