"""Хранилище скрипта диалога v2.0.

ВАЖНО: зашитый «боевой» скрипт продаж намеренно удалён и НЕ переносится.
Движок v2.0 поставляется пустым — текстовое наполнение скрипта задаётся
отдельно (раздел «Скрипты диалога»). Здесь остаётся только структура:
список фаз и пустой словарь реплик, который наполняется при настройке.
"""

# Фазы скриптового алгоритма v2.0 (порядок прохождения диалога).
PHASES: tuple[str, ...] = (
    "secretary",
    "lpr_greeting",
    "lpr_main",
    "qualification",
    "closed",
)

# Плейсхолдер, который произносит робот, пока скрипт не настроен.
SCRIPT_NOT_CONFIGURED: str = "[Скрипт диалога не настроен]"

# Реплики скрипта по коду узла. Пусто по умолчанию — наполняется при настройке
# скрипта в разделе «Скрипты диалога».
SCRIPT: dict[str, str] = {}
